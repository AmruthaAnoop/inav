# Test Environment Variables
# Copy this to .env.test for testing

NODE_ENV=test
PORT=5001

# Test Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=payment_collection_test

# CORS
CORS_ORIGIN=http://localhost:19000

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# JWT (not used in tests yet)
JWT_SECRET=test_secret_key
JWT_EXPIRE=7d
